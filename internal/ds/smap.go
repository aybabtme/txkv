package ds

// GENERATED CODE, DO NOT EDIT
// This code was generated by a tool.
//
// 	github.com/aybabtme/datagen
//
// The command that generated this was:
//
//	datagen smap --key []byte --val []byte

import "bytes"

// WARNING: using []byte as keys can lead to undefined behavior if the
// []byte are modified after insertion!!!
func (r SortedBytesToBytesMap) compare(a, b []byte) int { return bytes.Compare(a, b) }

// SortedBytesToBytesMap is a sorted map built on a left leaning red black balanced
// search sorted map. It stores []byte values, keyed by []byte.
type SortedBytesToBytesMap struct {
	root *nodeBytesToBytes
}

// NewSortedBytesToBytesMap creates a sorted map.
func NewSortedBytesToBytesMap() *SortedBytesToBytesMap { return &SortedBytesToBytesMap{} }

// IsEmpty tells if the sorted map contains no key/value.
func (r SortedBytesToBytesMap) IsEmpty() bool {
	return r.root == nil
}

// Size of the sorted map.
func (r SortedBytesToBytesMap) Size() int { return r.root.size() }

// Clear all the values in the sorted map.
func (r *SortedBytesToBytesMap) Clear() { r.root = nil }

// Put a value in the sorted map at key `k`. The old value at `k` is returned
// if the key was already present.
func (r *SortedBytesToBytesMap) Put(k []byte, v []byte) (old []byte, overwrite bool) {
	r.root, old, overwrite = r.put(r.root, k, func() []byte { return v }, func(_ []byte) []byte { return v })
	return
}

// Mutate is like a Put when `k` isn't defined, but allows you to create or mutate the value found at the location of `k`.
func (r *SortedBytesToBytesMap) Mutate(k []byte, creator func() []byte, mutator func(old []byte) []byte) {
	r.root, _, _ = r.put(r.root, k, creator, mutator)
}

func (r *SortedBytesToBytesMap) put(h *nodeBytesToBytes, k []byte, create func() []byte, mutate func(old []byte) []byte) (_ *nodeBytesToBytes, old []byte, overwrite bool) {
	if h == nil {
		n := &nodeBytesToBytes{key: k, val: create(), n: 1, colorRed: true}
		return n, old, overwrite
	}

	cmp := r.compare(k, h.key)
	if cmp < 0 {
		h.left, old, overwrite = r.put(h.left, k, create, mutate)
	} else if cmp > 0 {
		h.right, old, overwrite = r.put(h.right, k, create, mutate)
	} else {
		overwrite = true
		old = h.val
		h.val = mutate(old)
	}

	if h.right.isRed() && !h.left.isRed() {
		h = r.rotateLeft(h)
	}
	if h.left.isRed() && h.left.left.isRed() {
		h = r.rotateRight(h)
	}
	if h.left.isRed() && h.right.isRed() {
		r.flipColors(h)
	}
	h.n = h.left.size() + h.right.size() + 1
	return h, old, overwrite
}

// Get a value from the sorted map at key `k`. Returns false
// if the key doesn't exist.
func (r SortedBytesToBytesMap) Get(k []byte) ([]byte, bool) {
	return r.loopGet(r.root, k)
}

func (r SortedBytesToBytesMap) loopGet(h *nodeBytesToBytes, k []byte) (v []byte, ok bool) {
	for h != nil {
		cmp := r.compare(k, h.key)
		if cmp == 0 {
			return h.val, true
		} else if cmp < 0 {
			h = h.left
		} else if cmp > 0 {
			h = h.right
		}
	}
	return
}

// Has tells if a value exists at key `k`. This is short hand for `Get.
func (r SortedBytesToBytesMap) Has(k []byte) bool {
	_, ok := r.loopGet(r.root, k)
	return ok
}

// Min returns the smallest key/value in the sorted map, if it exists.
func (r SortedBytesToBytesMap) Min() (k []byte, v []byte, ok bool) {
	if r.root == nil {
		return
	}
	h := r.min(r.root)
	return h.key, h.val, true
}

func (r SortedBytesToBytesMap) min(x *nodeBytesToBytes) *nodeBytesToBytes {
	if x.left == nil {
		return x
	}
	return r.min(x.left)
}

// Max returns the largest key/value in the sorted map, if it exists.
func (r SortedBytesToBytesMap) Max() (k []byte, v []byte, ok bool) {
	if r.root == nil {
		return
	}
	h := r.max(r.root)
	return h.key, h.val, true
}

func (r SortedBytesToBytesMap) max(x *nodeBytesToBytes) *nodeBytesToBytes {
	if x.right == nil {
		return x
	}
	return r.max(x.right)
}

// Floor returns the largest key/value in the sorted map that is smaller than
// `k`.
func (r SortedBytesToBytesMap) Floor(key []byte) (k []byte, v []byte, ok bool) {
	x := r.floor(r.root, key)
	if x == nil {
		return
	}
	return x.key, x.val, true
}

func (r SortedBytesToBytesMap) floor(h *nodeBytesToBytes, k []byte) *nodeBytesToBytes {
	if h == nil {
		return nil
	}
	cmp := r.compare(k, h.key)
	if cmp == 0 {
		return h
	}
	if cmp < 0 {
		return r.floor(h.left, k)
	}
	t := r.floor(h.right, k)
	if t != nil {
		return t
	}
	return h
}

// Ceiling returns the smallest key/value in the sorted map that is larger than
// `k`.
func (r SortedBytesToBytesMap) Ceiling(key []byte) (k []byte, v []byte, ok bool) {
	x := r.ceiling(r.root, key)
	if x == nil {
		return
	}
	return x.key, x.val, true
}

func (r SortedBytesToBytesMap) ceiling(h *nodeBytesToBytes, k []byte) *nodeBytesToBytes {
	if h == nil {
		return nil
	}
	cmp := r.compare(k, h.key)
	if cmp == 0 {
		return h
	}
	if cmp > 0 {
		return r.ceiling(h.right, k)
	}
	t := r.ceiling(h.left, k)
	if t != nil {
		return t
	}
	return h
}

// Select key of rank k, meaning the k-th biggest []byte in the sorted map.
func (r SortedBytesToBytesMap) Select(key int) (k []byte, v []byte, ok bool) {
	x := r.nodeselect(r.root, key)
	if x == nil {
		return
	}
	return x.key, x.val, true
}

func (r SortedBytesToBytesMap) nodeselect(x *nodeBytesToBytes, k int) *nodeBytesToBytes {
	if x == nil {
		return nil
	}
	t := x.left.size()
	if t > k {
		return r.nodeselect(x.left, k)
	} else if t < k {
		return r.nodeselect(x.right, k-t-1)
	} else {
		return x
	}
}

// Rank is the number of keys less than `k`.
func (r SortedBytesToBytesMap) Rank(k []byte) int {
	return r.keyrank(k, r.root)
}

func (r SortedBytesToBytesMap) keyrank(k []byte, h *nodeBytesToBytes) int {
	if h == nil {
		return 0
	}
	cmp := r.compare(k, h.key)
	if cmp < 0 {
		return r.keyrank(k, h.left)
	} else if cmp > 0 {
		return 1 + h.left.size() + r.keyrank(k, h.right)
	} else {
		return h.left.size()
	}
}

// Keys visit each keys in the sorted map, in order.
// It stops when visit returns false.
func (r SortedBytesToBytesMap) Keys(visit func([]byte, []byte) bool) {
	min, _, ok := r.Min()
	if !ok {
		return
	}
	// if the min exists, then the max must exist
	max, _, _ := r.Max()
	r.RangedKeys(min, max, visit)
}

// RangedKeys visit each keys between lo and hi in the sorted map, in order.
// It stops when visit returns false.
func (r SortedBytesToBytesMap) RangedKeys(lo, hi []byte, visit func([]byte, []byte) bool) {
	r.keys(r.root, visit, lo, hi)
}

func (r SortedBytesToBytesMap) keys(h *nodeBytesToBytes, visit func([]byte, []byte) bool, lo, hi []byte) bool {
	if h == nil {
		return true
	}
	cmplo := r.compare(lo, h.key)
	cmphi := r.compare(hi, h.key)
	if cmplo < 0 {
		if !r.keys(h.left, visit, lo, hi) {
			return false
		}
	}
	if cmplo <= 0 && cmphi >= 0 {
		if !visit(h.key, h.val) {
			return false
		}
	}
	if cmphi > 0 {
		if !r.keys(h.right, visit, lo, hi) {
			return false
		}
	}
	return true
}

// DeleteMin removes the smallest key and its value from the sorted map.
func (r *SortedBytesToBytesMap) DeleteMin() (oldk []byte, oldv []byte, ok bool) {
	r.root, oldk, oldv, ok = r.deleteMin(r.root)
	if !r.IsEmpty() {
		r.root.colorRed = false
	}
	return
}

func (r *SortedBytesToBytesMap) deleteMin(h *nodeBytesToBytes) (_ *nodeBytesToBytes, oldk []byte, oldv []byte, ok bool) {
	if h == nil {
		return nil, oldk, oldv, false
	}

	if h.left == nil {
		return nil, h.key, h.val, true
	}
	if !h.left.isRed() && !h.left.left.isRed() {
		h = r.moveRedLeft(h)
	}
	h.left, oldk, oldv, ok = r.deleteMin(h.left)
	return r.balance(h), oldk, oldv, ok
}

// DeleteMax removes the largest key and its value from the sorted map.
func (r *SortedBytesToBytesMap) DeleteMax() (oldk []byte, oldv []byte, ok bool) {
	r.root, oldk, oldv, ok = r.deleteMax(r.root)
	if !r.IsEmpty() {
		r.root.colorRed = false
	}
	return
}

func (r *SortedBytesToBytesMap) deleteMax(h *nodeBytesToBytes) (_ *nodeBytesToBytes, oldk []byte, oldv []byte, ok bool) {
	if h == nil {
		return nil, oldk, oldv, ok
	}
	if h.left.isRed() {
		h = r.rotateRight(h)
	}
	if h.right == nil {
		return nil, h.key, h.val, true
	}
	if !h.right.isRed() && !h.right.left.isRed() {
		h = r.moveRedRight(h)
	}
	h.right, oldk, oldv, ok = r.deleteMax(h.right)
	return r.balance(h), oldk, oldv, ok
}

// Delete key `k` from sorted map, if it exists.
func (r *SortedBytesToBytesMap) Delete(k []byte) (old []byte, ok bool) {
	if r.root == nil {
		return
	}
	r.root, old, ok = r.delete(r.root, k)
	if !r.IsEmpty() {
		r.root.colorRed = false
	}
	return
}

func (r *SortedBytesToBytesMap) delete(h *nodeBytesToBytes, k []byte) (_ *nodeBytesToBytes, old []byte, ok bool) {

	if h == nil {
		return h, old, false
	}

	if r.compare(k, h.key) < 0 {
		if h.left == nil {
			return h, old, false
		}

		if !h.left.isRed() && !h.left.left.isRed() {
			h = r.moveRedLeft(h)
		}

		h.left, old, ok = r.delete(h.left, k)
		h = r.balance(h)
		return h, old, ok
	}

	if h.left.isRed() {
		h = r.rotateRight(h)
	}

	if r.compare(k, h.key) == 0 && h.right == nil {
		return nil, h.val, true
	}

	if h.right != nil && !h.right.isRed() && !h.right.left.isRed() {
		h = r.moveRedRight(h)
	}

	if r.compare(k, h.key) == 0 {

		var subk []byte
		var subv []byte
		h.right, subk, subv, ok = r.deleteMin(h.right)

		old, h.key, h.val = h.val, subk, subv
		ok = true
	} else {
		h.right, old, ok = r.delete(h.right, k)
	}

	h = r.balance(h)
	return h, old, ok
}

// deletions

func (r *SortedBytesToBytesMap) moveRedLeft(h *nodeBytesToBytes) *nodeBytesToBytes {
	r.flipColors(h)
	if h.right.left.isRed() {
		h.right = r.rotateRight(h.right)
		h = r.rotateLeft(h)
		r.flipColors(h)
	}
	return h
}

func (r *SortedBytesToBytesMap) moveRedRight(h *nodeBytesToBytes) *nodeBytesToBytes {
	r.flipColors(h)
	if h.left.left.isRed() {
		h = r.rotateRight(h)
		r.flipColors(h)
	}
	return h
}

func (r *SortedBytesToBytesMap) balance(h *nodeBytesToBytes) *nodeBytesToBytes {
	if h.right.isRed() {
		h = r.rotateLeft(h)
	}
	if h.left.isRed() && h.left.left.isRed() {
		h = r.rotateRight(h)
	}
	if h.left.isRed() && h.right.isRed() {
		r.flipColors(h)
	}
	h.n = h.left.size() + h.right.size() + 1
	return h
}

func (r *SortedBytesToBytesMap) rotateLeft(h *nodeBytesToBytes) *nodeBytesToBytes {
	x := h.right
	h.right = x.left
	x.left = h
	x.colorRed = h.colorRed
	h.colorRed = true
	x.n = h.n
	h.n = 1 + h.left.size() + h.right.size()
	return x
}

func (r *SortedBytesToBytesMap) rotateRight(h *nodeBytesToBytes) *nodeBytesToBytes {
	x := h.left
	h.left = x.right
	x.right = h
	x.colorRed = h.colorRed
	h.colorRed = true
	x.n = h.n
	h.n = 1 + h.left.size() + h.right.size()
	return x
}

func (r *SortedBytesToBytesMap) flipColors(h *nodeBytesToBytes) {
	h.colorRed = !h.colorRed
	h.left.colorRed = !h.left.colorRed
	h.right.colorRed = !h.right.colorRed
}

// nodes

type nodeBytesToBytes struct {
	key         []byte
	val         []byte
	left, right *nodeBytesToBytes
	n           int
	colorRed    bool
}

func (x *nodeBytesToBytes) isRed() bool { return (x != nil) && (x.colorRed == true) }

func (x *nodeBytesToBytes) size() int {
	if x == nil {
		return 0
	}
	return x.n
}

